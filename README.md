## READ AND WRITE IMPLEMENTED

## GET AND POST API USED IN `reviewRoutes.js` and `gymRoutes.js`
  - Used `POST API` to allow users to post reviews.
  - Used `GET API` to fetch all the gyms and their reviews. 
